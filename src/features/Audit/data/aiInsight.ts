import type { AuditAiInsight } from '../types'

/** AI audit summary banner. */
export const AUDIT_AI_INSIGHT: AuditAiInsight = {
  title: '✦ AI audit summary',
  bodyHtml:
    '<strong>18,420 actions logged today</strong> across the platform. All actions digitally signed with SHA-256 hash chain for legal admissibility. <strong>No anomalies detected</strong> — all user actions within expected patterns. <strong>14 cases finalized</strong>, 8 assessments generated, 6 evidence packs signed. Audit chain integrity: <strong style="color:var(--green)">100% verified</strong>.',
}
