import type { ReportsAiInsightData } from '../types'

/** AI report-assistant footer (uses the app's existing .ai-insight styling). */
export const REPORTS_AI_INSIGHT: ReportsAiInsightData = {
  title: '✦ AI report assistant',
  bodyHtml:
    'Two reports need attention this week: <strong>CEA reliability indices</strong> (due 10 May, 4 days) is auto-generating but SAIFI data is still being collated by Ops; <strong>UPERC ARR petition FY26-27</strong> (due 15 May, 9 days) needs CMD sign-off on proposed tariff revision of 4.2%. Recommend escalating both via "Today\'s Fire" on Executive page.',
}
